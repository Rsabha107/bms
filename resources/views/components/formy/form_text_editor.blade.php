<div class="{{ $class }}">
    <label class="{{ $classLabel }}" for="{{ $elementId }}">{{ $label }}</label>
    <textarea class="tinymce" name="{{ $name }}" data-tinymce="{}" {{ $required }} {{ $disabled }} style="height: 50px;">{{ $inputValue }}</textarea>
</div>
